import React from 'react';

const Plane = (twoWay) => {
  return (
      <span className="glyphicon glyphicon-plane" style={{transform: 'rotate(90deg)', fontSize: '20px'}}
            aria-hidden="true"/>
  )
};

export default Plane;
